import React from 'react'
import ReactDOM from 'react-dom/client'
import ScreenplayApp from './screenplay-writer.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <ScreenplayApp />
    </React.StrictMode>
)
